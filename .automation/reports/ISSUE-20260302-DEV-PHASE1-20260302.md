# ISSUE-20260302 Development Report (Phase 1) / 开发报告（问题1）

Date: 2026-03-02
Task: ISSUE-20260302
Stage: Dev + Test (Phase 1)

## EN (for agents)

### Scope
Implement Issue 1 only: stale/unreliable news handling.

### Code Changes
1. Prompt hard constraints for news-related agents:
   - Updated stock news and sector news prompts to enforce 72h default recency window.
   - Added controlled fallback to 7d with explicit expansion note requirement.
   - Required `source`, `publishedAt`, and `crawledAt` in `evidence`.
2. Retrieval filtering and ranking in agent context:
   - Added `StockAgentNewsContextPolicy` in `StockAgentOrchestrator.cs`.
   - Implemented 72h preferred window, fallback to 7d when insufficient messages.
   - Added source trust preference + composite ranking (`recency + source + relevance`).
   - Added policy metadata into context (`NewsPolicy`).
3. Structured output fallback guardrail:
   - In `StockAgentResultNormalizer`, for `stock_news` and `sector_news`, if no usable evidence
     (`source` + parseable `publishedAt`) exists, auto-downgrade to neutral/watch mode.

### Tests Added/Updated
- Updated:
  - `StockAgentPromptBuilderTests`
  - `StockAgentResultNormalizerTests`
- Added:
  - `StockAgentNewsContextPolicyTests`

### Test Commands & Results
1) Focused tests (first run)
- Command:
  - `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj --filter "StockAgentPromptBuilderTests|StockAgentResultNormalizerTests|StockAgentNewsContextPolicyTests|StockNewsImpactServiceTests"`
- Result:
  - FAIL (build locked by running `SimplerJiangAiAgent.Api` process PID 50720)
  - Action: `Stop-Process -Id 50720 -Force`

2) Focused tests (rerun)
- Same command as above
- Result: PASS (13/13)

3) Full backend test project
- Command:
  - `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj`
- Result: PASS (38/38)

4) Frontend unit tests (required before Edge)
- Command:
  - `cd frontend && npm run test:unit`
- Result: PASS (23/23)

5) Edge Playwright check (latest feature flow)
- Pre-steps:
  - `cd frontend && npm run build` (PASS)
  - start backend on `http://localhost:5119`
- Command:
  - `cd frontend/scripts && UI_BASE_URL=http://localhost:5119 node edge-check-goal007.mjs`
- First run:
  - `success=false` (UI interaction timing/element readiness not satisfied)
- Rerun:
  - `success=true`
  - key fields: `hasStockName=true`, `hasAgentCard=true`, `hasRawJson=true`, `chatInteractive=true`, `consoleErrors=[]`
- Evidence:
  - `.automation/reports/edge-goal007-final/summary.json`
  - `.automation/reports/edge-goal007-final/ui-goal007-final.png`

6) Backend log check after UI run
- Checked active backend terminal output around UI requests.
- Observed `/api/stocks/detail`, `/api/stocks/news/impact`, `/api/stocks/chat/...`, `/api/stocks/agents/single` returned `200` without runtime exception.

### Notes
- No frontend code changes in this phase; Edge/UI checks not required for this backend-only scope.

### Follow-up Closure (2026-03-02)
1. Implemented frontend stale evidence highlighting:
  - Added `publishedAt` stale-risk rendering (`>72h`) in `StockAgentPanels.vue`.
  - Added `StockAgentPanels.spec.js` to verify expired-risk marker rendering.
2. Ran replay-style validation for Issue-1 acceptance A/B:
  - Script queried `/api/stocks/news/impact` for 5 symbols.
  - Output: `.automation/reports/ISSUE-20260302-REPLAY-PHASE1.json`.
  - Aggregate result: `ratio72h=100`, `traceableRatio=100`, `passA=true`, `passB=true`.
3. Re-ran frontend unit tests:
  - `npm run test:unit` => PASS (24/24).

---

## 中文（给用户）

### 范围
本次仅实现问题1：资讯“新鲜度/可靠性”治理。

### 代码改动
1. Prompt 强约束（资讯类 Agent）
   - 个股资讯与板块资讯 Prompt 增加：默认 72h 时效窗口。
   - 当有效证据不足时允许扩窗到 7d，并要求在结果中明确标注扩窗。
   - `evidence` 强制包含 `source`、`publishedAt`、`crawledAt`。
2. 后端检索过滤与排序
   - 在 `StockAgentOrchestrator.cs` 新增 `StockAgentNewsContextPolicy`。
   - 实现 72h 优先、证据不足扩窗 7d。
   - 引入来源可信度偏好 + 综合评分排序（时效+来源+相关性）。
   - 在上下文中加入 `NewsPolicy` 元信息，供 Agent 理解窗口策略。
3. 输出结构化兜底降级
   - 在 `StockAgentResultNormalizer` 中，对 `stock_news`/`sector_news` 增加校验：
   - 若无可用证据（无来源或发布时间不可解析），自动降级为“信息不足，建议观望”。

### 新增/更新测试
- 更新：
  - `StockAgentPromptBuilderTests`
  - `StockAgentResultNormalizerTests`
- 新增：
  - `StockAgentNewsContextPolicyTests`

### 测试命令与结果
1）定向测试（首次）
- 命令：
  - `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj --filter "StockAgentPromptBuilderTests|StockAgentResultNormalizerTests|StockAgentNewsContextPolicyTests|StockNewsImpactServiceTests"`
- 结果：
  - 失败（运行中的 `SimplerJiangAiAgent.Api` 进程 PID 50720 锁定编译输出）
  - 处理：执行 `Stop-Process -Id 50720 -Force`

2）定向测试（重跑）
- 同上命令
- 结果：通过（13/13）

3）后端全量测试
- 命令：
  - `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj`
- 结果：通过（38/38）

4）前端单测（按顺序在 Edge 前执行）
- 命令：
  - `cd frontend && npm run test:unit`
- 结果：通过（23/23）

5）Edge Playwright（最新功能流程）
- 前置：
  - `cd frontend && npm run build`（通过）
  - 启动后端 `http://localhost:5119`
- 命令：
  - `cd frontend/scripts && UI_BASE_URL=http://localhost:5119 node edge-check-goal007.mjs`
- 首次运行：
  - `success=false`（页面交互时序未满足）
- 重跑结果：
  - `success=true`
  - 关键字段：`hasStockName=true`、`hasAgentCard=true`、`hasRawJson=true`、`chatInteractive=true`、`consoleErrors=[]`
- 证据文件：
  - `.automation/reports/edge-goal007-final/summary.json`
  - `.automation/reports/edge-goal007-final/ui-goal007-final.png`

6）UI 后端日志检查
- 已检查后端终端输出，`/api/stocks/detail`、`/api/stocks/news/impact`、`/api/stocks/chat/...`、`/api/stocks/agents/single` 均返回 `200`，未见运行时异常。

### 备注
- 本阶段未改前端代码，按规则无需执行 Edge/UI 验证。

### 收尾补充（2026-03-02）
1. 已补前端“证据发布时间过期风险”可视化：
  - 在 `StockAgentPanels.vue` 中对 `publishedAt` 超过72小时标注“过期风险”并高亮。
  - 新增 `StockAgentPanels.spec.js` 验证过期标记渲染。
2. 已执行问题1验收A/B回放统计：
  - 调用 5 个标的的 `/api/stocks/news/impact`。
  - 结果文件：`.automation/reports/ISSUE-20260302-REPLAY-PHASE1.json`。
  - 汇总结果：`ratio72h=100`、`traceableRatio=100`、`passA=true`、`passB=true`。
3. 已重跑前端单测：
  - `npm run test:unit` => 通过（24/24）。
