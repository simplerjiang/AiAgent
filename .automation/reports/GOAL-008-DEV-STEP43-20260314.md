# GOAL-008 Step 4.3 Development Report (2026-03-14)

## EN
### Summary
Completed Step 4.3 of the trading-plan engine. The backend now evaluates pending plans from local cached quote/minute data only, persists trading-plan events for audit, exposes alert read APIs, and the frontend now shows alert summaries on both the global trading-plan board and the current-stock plan card via short polling.

### Implementation
- Backend:
  - Added `TradingPlanEvent` with tolerant enum conversion and EF mapping in `AppDbContext`.
  - Added additive schema support plus migration for `TradingPlanEvents`.
  - Added `TradingPlanTriggerOptions`, `TradingPlanTriggerWorker`, and `TradingPlanTriggerService`.
  - Added `GET /api/stocks/plans/alerts` in `StocksModule` and a new `TradingPlanEventItemDto`.
  - Implemented deterministic state transitions:
    - `Pending -> Invalid` when invalid condition matches
    - `Pending -> Triggered` when trigger condition matches
    - `Invalid` wins when both match
  - Added warning-only recent minute volume-divergence event emission without changing plan status.
- Frontend:
  - Added plan-alert workspace state, request tokens, abort handling, and `fetchTradingPlanAlerts(...)`.
  - Added `refreshTradingPlanBoard(...)` / `refreshTradingPlanSection(...)` so plan list and alert summary refresh together.
  - Added alert summary rendering to the global board and current-plan card.
  - Added short polling for the active stock plan section and cleaned up the new abort controller/timer paths on unmount.
  - Added alert summary styling.
- Test updates:
  - Added `TradingPlanTriggerServiceTests.cs` for trigger, invalid, priority, idempotency, and divergence warning coverage.
  - Extended `StockInfoTab.spec.js` with alert-summary rendering/refresh coverage and updated shared mocks for the new alerts endpoint.

### Validation
- Backend targeted tests:
  - Command: `dotnet test .\backend\SimplerJiangAiAgent.Api.Tests\SimplerJiangAiAgent.Api.Tests.csproj --filter "TradingPlanTriggerServiceTests|TradingPlanServicesTests|HighFrequencyQuoteServiceTests"`
  - Result: pass `18/18`
- Frontend targeted tests:
  - Command: `npm run test:unit -- src/modules/stocks/StockInfoTab.spec.js`
  - Result: pass `36/36`
- Frontend production build:
  - Command: `npm run build`
  - Result: pass
- SQL schema verification:
  - Command: `sqlcmd -S 'np:\\.\pipe\MSSQL$SQLEXPRESS\sql\query' -d SimplerJiangAiAgent -Q "... TradingPlanEvents ..."`
  - Result: local `TradingPlanEvents` table exists and required indexes `IX_TradingPlanEvents_PlanId_OccurredAt` / `IX_TradingPlanEvents_Symbol_OccurredAt` are present
  - Note: the live table already contains extra legacy columns (`VersionId`, `Strategy`, `Reason`, `CreatedAt`) in addition to the new Step 4.3 fields; no mismatch was observed for the required columns/indexes used by this step.
- Browser MCP on backend-served frontend:
  - Started backend-served app with `start-all.bat`
  - Verified `http://localhost:5119/api/health` => `{"status":"ok"}`
  - Verified fresh network calls returned `200` for:
    - `/api/stocks/plans?take=20`
    - `/api/stocks/plans/alerts?take=20`
    - `/api/stocks/plans?symbol=sz000021`
    - `/api/stocks/plans/alerts?symbol=sz000021&take=20`
  - Clicked recent history to load `sz000021`, then triggered both board and current-plan refresh actions
  - Result: board alert summary rendered on the live page, refresh actions issued the expected requests, and browser console reported no errors

### Notes / Risks
- Live browser data did not include a current-stock alert card for the selected symbol at the moment of validation, but the endpoint returned `200`, the frontend unit test covers current-card rendering, and the global board rendered a live event summary successfully.
- The frontend build still warns that the main JS chunk exceeds 500 kB; this is unchanged and not introduced by Step 4.3.

## ZH
### 摘要
已完成交易计划引擎 Step 4.3。后端现在只基于本地 quote/minute 缓存评估 `Pending` 计划，持久化交易计划事件用于审计，并暴露告警读取接口；前端则通过短轮询把最新告警摘要展示到“交易计划总览”和“当前交易计划”卡片上。

### 实现内容
- 后端：
  - 新增 `TradingPlanEvent`，并在 `AppDbContext` 中补齐宽容枚举转换与 EF 映射。
  - 新增 `TradingPlanEvents` 的 additive schema 初始化与 migration。
  - 新增 `TradingPlanTriggerOptions`、`TradingPlanTriggerWorker`、`TradingPlanTriggerService`。
  - 在 `StocksModule` 中新增 `GET /api/stocks/plans/alerts` 与 `TradingPlanEventItemDto`。
  - 实现确定性状态机：
    - 命中失效条件时 `Pending -> Invalid`
    - 命中触发条件时 `Pending -> Triggered`
    - 同轮同时命中时 `Invalid` 优先
  - 增加最近分钟量价背离 `Warning` 事件，但不直接改变计划状态。
- 前端：
  - 新增 plan alert 的 workspace 状态、request token、abort 处理与 `fetchTradingPlanAlerts(...)`。
  - 新增 `refreshTradingPlanBoard(...)` / `refreshTradingPlanSection(...)`，让计划列表与告警摘要一起刷新。
  - 在跨股票总览卡和当前股票计划卡中展示最新告警摘要。
  - 为当前激活股票增加计划短轮询，并在组件卸载时清理新的 abort controller 与 timer。
  - 新增告警摘要样式。
- 测试补充：
  - 新增 `TradingPlanTriggerServiceTests.cs`，覆盖触发、失效、优先级、幂等和背离 warning。
  - 扩展 `StockInfoTab.spec.js`，增加告警摘要渲染/刷新用例，并同步更新共享 mock，适配新的 alerts 接口。

### 验证结果
- 后端定向测试：
  - 命令：`dotnet test .\backend\SimplerJiangAiAgent.Api.Tests\SimplerJiangAiAgent.Api.Tests.csproj --filter "TradingPlanTriggerServiceTests|TradingPlanServicesTests|HighFrequencyQuoteServiceTests"`
  - 结果：通过 `18/18`
- 前端定向测试：
  - 命令：`npm run test:unit -- src/modules/stocks/StockInfoTab.spec.js`
  - 结果：通过 `36/36`
- 前端生产构建：
  - 命令：`npm run build`
  - 结果：通过
- SQL 结构核验：
  - 命令：`sqlcmd -S 'np:\\.\pipe\MSSQL$SQLEXPRESS\sql\query' -d SimplerJiangAiAgent -Q "... TradingPlanEvents ..."`
  - 结果：本地 `TradingPlanEvents` 表存在，且 `IX_TradingPlanEvents_PlanId_OccurredAt` / `IX_TradingPlanEvents_Symbol_OccurredAt` 索引已存在
  - 备注：线上本地表还带有旧字段 `VersionId`、`Strategy`、`Reason`、`CreatedAt`，但本次 Step 4.3 依赖的字段与索引均已齐全，没有发现结构不匹配
- 后端托管页面 Browser MCP：
  - 使用 `start-all.bat` 启动本地应用
  - 已验证 `http://localhost:5119/api/health` 返回 `{"status":"ok"}`
  - 已验证以下新请求均返回 `200`：
    - `/api/stocks/plans?take=20`
    - `/api/stocks/plans/alerts?take=20`
    - `/api/stocks/plans?symbol=sz000021`
    - `/api/stocks/plans/alerts?symbol=sz000021&take=20`
  - 已点击最近查询加载 `sz000021`，并触发总览与当前计划两处刷新操作
  - 结果：真实页面上“交易计划总览”已渲染告警摘要，刷新交互能打出预期请求，浏览器控制台无报错

### 备注 / 风险
- 本次浏览器实测时，选中的当前股票没有现成的 live alert 卡片数据，但接口返回正常，前端单测已覆盖当前卡片渲染，且跨股票总览已成功显示 live 事件摘要。
- 前端 build 仍有主包超过 500 kB 的既有 warning，这不是 Step 4.3 新引入的问题。