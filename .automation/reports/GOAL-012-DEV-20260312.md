# GOAL-012 Development Report (EN + ZH)

## EN
### Summary
Completed GOAL-012 Step 1: the stock info page now uses a decoupled terminal layout with a dedicated chart-focused `TerminalView` and a right-side `CopilotPanel` for AI functions.

### Implemented Changes
- Added `frontend/src/modules/stocks/TerminalView.vue`.
- Added `frontend/src/modules/stocks/CopilotPanel.vue`.
- Refactored `frontend/src/modules/stocks/StockInfoTab.vue` into:
  - top control deck for query/history controls
  - left `TerminalView` for quote summary, message tape, K-line/minute chart area
  - right `CopilotPanel` for news impact, multi-agent panels, and chat
  - focus-mode toggle to hide/show the AI sidebar
- Added frontend tests in `frontend/src/modules/stocks/StockInfoTab.spec.js` for:
  - terminal workspace rendering
  - focus-mode collapse behavior

### Validation Results
1. Frontend unit tests first:
- Command: `npm --prefix frontend run test:unit -- src/modules/stocks/StockInfoTab.spec.js`
- Result: Passed (13/13).

2. Frontend production build:
- Command: `npm --prefix frontend run build`
- Result: Passed.
- Note: Vite reported a chunk-size warning for the existing bundle, but build output succeeded.

3. Edge MCP:
- Not executed in this turn.
- Reason: no dedicated GOAL-012 interaction script or active backend-served validation target was prepared inside this session after the frontend-only refactor.

### Scope Control
- Did not start GOAL-013 Step 2.
- Did not change backend code.
- Kept AI requests user-triggered; this change focused on visual and interaction decoupling only.

### Rework Update (Step 1.1)
- Reviewer rejected the first submission because the search/query block still consumed too much vertical document-flow space.
- Reworked `StockInfoTab.vue` again to:
  - convert the search/history area into a compact sticky toolbar
  - collapse recent history into a horizontal quick-switch ribbon
  - expand `workspace-grid` to a viewport-based minimum height
  - increase `StockCharts.vue` chart containers so K-line and minute views stretch into the remaining height
- Additional validation:
  - `npm --prefix frontend run test:unit -- src/modules/stocks/StockInfoTab.spec.js` -> Passed (13/13)
  - `npm --prefix frontend run build` -> Passed

## ZH
### 摘要
已完成 GOAL-012 Step 1：股票信息页已改造成终端化布局，主视区专注图表与行情，右侧 `CopilotPanel` 集中承载 AI 相关能力。

### 实施内容
- 新增 `frontend/src/modules/stocks/TerminalView.vue`。
- 新增 `frontend/src/modules/stocks/CopilotPanel.vue`。
- 重构 `frontend/src/modules/stocks/StockInfoTab.vue`：
  - 顶部改为查询/历史控制区
  - 左侧 `TerminalView` 负责行情摘要、消息带、K 线/分时图表区
  - 右侧 `CopilotPanel` 负责资讯影响、多 Agent 分析与聊天
  - 新增专注模式开关，可一键隐藏或恢复 AI 侧栏
- 更新 `frontend/src/modules/stocks/StockInfoTab.spec.js`，新增：
  - 终端布局渲染测试
  - 专注模式折叠行为测试

### 校验结果
1. 先跑前端单测：
- 命令：`npm --prefix frontend run test:unit -- src/modules/stocks/StockInfoTab.spec.js`
- 结果：通过（13/13）。

2. 前端构建：
- 命令：`npm --prefix frontend run build`
- 结果：通过。
- 说明：Vite 输出了既有包体积偏大的 warning，但不影响本次构建成功。

3. Edge MCP：
- 本轮未执行。
- 原因：当前会话中没有为 GOAL-012 预置专门的交互脚本，也没有在本轮前端重构后补起一个后端托管的页面验证目标。

### 范围控制
- 没有开始 GOAL-013 Step 2。
- 没有改动后端代码。
- AI 请求仍保持用户主动触发，本次仅完成界面与交互层的解耦。

### 返工更新（Step 1.1）
- Reviewer 打回原因：首版提交中“标的查询/历史查询”仍占据过多纵向文档流高度，主图表视区不够大。
- 已再次重构 `StockInfoTab.vue`：
  - 将查询/历史控制区改为顶部紧凑 `sticky toolbar`
  - 将最近历史改为横向 `history ribbon` 快捷切换
  - 将 `workspace-grid` 改为基于视口剩余高度的最小高度布局
  - 提升 `StockCharts.vue` 中 K 线与分时图容器高度，使图表真正吃满剩余空间
- 增量验证结果：
  - `npm --prefix frontend run test:unit -- src/modules/stocks/StockInfoTab.spec.js` -> 通过（13/13）
  - `npm --prefix frontend run build` -> 通过
