# MANUAL-20260406-BUGFIX-R1-20260407

## EN

### Scope

- Follow-up on Bug 1 for the archive pending AI cleaning flow.
- Focus of this round: stop the first recoverable no-progress/error round from surfacing as a terminal stop, add explicit pause/restart controls, and expose bounded recent request/response diagnostics.

### Actions

- Updated `backend/SimplerJiangAiAgent.Api/Infrastructure/Jobs/LocalFactAiEnrichmentService.cs`:
  - added explicit `paused / failed / completed / idle / running` archive job state handling,
  - added bounded recoverable retry/backoff for first-round no-progress and recoverable exceptions,
  - added explicit `PauseAsync` and `RestartAsync`,
  - added recent archive job events for request/response/progress/error visibility,
  - fixed the paused-before-first-round edge case so a paused run is still resumable even when remaining counts are still zero.
- Updated `backend/SimplerJiangAiAgent.Api/Modules/Stocks/StocksModule.cs` to expose pause and restart endpoints for the archive job.
- Updated `backend/SimplerJiangAiAgent.Api.Tests/LocalFactAiEnrichmentServiceTests.cs` with coordinator regressions for auto-retry, pause/resume, and restart behavior.
- Updated `frontend/src/modules/stocks/NewsArchiveTab.vue`:
  - normalized the richer backend status payload,
  - kept first recoverable no-progress in a running warning state instead of falsely ending the run,
  - added explicit pause and restart controls,
  - added a collapsible recent request/response details panel,
  - aligned paused-state resume handling with the backend edge-case fix.
- Updated `frontend/src/modules/stocks/NewsArchiveTab.spec.js` with regressions for auto-retry warning state, pause/resume, restart, and diagnostics rendering.
- Updated `README.UserAgentTest.md` so the manual archive-cleaning checklist now covers pause/restart controls, diagnostics details, and the non-terminal auto-retry behavior.

### Test Commands And Results

- Command: `npm --prefix .\frontend run test:unit -- src/modules/stocks/NewsArchiveTab.spec.js`
- Result: passed, 9/9 tests.

- Command: `dotnet test .\backend\SimplerJiangAiAgent.Api.Tests\SimplerJiangAiAgent.Api.Tests.csproj --filter "FullyQualifiedName~LocalFactAiEnrichmentServiceTests" -v minimal`
- Result: passed, 15/15 tests.

### Outstanding Validation

- Browser or live source/package validation was not run in this session.
- Because that higher-level acceptance is still missing, this task remains in `in-progress` rather than closed.

## ZH

### 范围

- 本轮是 Bug 1 的 follow-up，继续收口资讯库待清洗后台链路。
- 目标是三件事：首轮可恢复无进展/异常不能直接表现成终止态；增加显式暂停/重启控制；把最近请求/响应诊断信息暴露到页面。

### 本轮动作

- 修改 `backend/SimplerJiangAiAgent.Api/Infrastructure/Jobs/LocalFactAiEnrichmentService.cs`：
  - 明确区分 `paused / failed / completed / idle / running` 状态，
  - 给首轮无进展和可恢复异常补上有上限的自动重试与退避，
  - 新增显式 `PauseAsync` / `RestartAsync`，
  - 给状态返回补上最近请求/响应/进度/错误事件，
  - 修掉“首轮还没产出 remaining 计数就被暂停”时无法继续的边界问题，保证 paused 状态仍可恢复。
- 修改 `backend/SimplerJiangAiAgent.Api/Modules/Stocks/StocksModule.cs`，补上 archive job 的暂停和重启 endpoint。
- 修改 `backend/SimplerJiangAiAgent.Api.Tests/LocalFactAiEnrichmentServiceTests.cs`，新增 auto-retry、pause/resume、restart 三类 coordinator 回归。
- 修改 `frontend/src/modules/stocks/NewsArchiveTab.vue`：
  - 兼容新的后端状态 payload，
  - 首轮可恢复无进展时继续展示“运行中 + 自动重试 warning”，不再误报“本轮后台清洗已结束”，
  - 增加显式 `暂停后台清洗` / `重新开始清洗` 控制，
  - 增加可折叠的最近请求 / 响应详情面板，
  - 前端同时兼容“暂停发生在首轮计数之前”的恢复场景。
- 修改 `frontend/src/modules/stocks/NewsArchiveTab.spec.js`，补上 auto-retry warning、pause/resume、restart、诊断详情面板的回归测试。
- 修改 `README.UserAgentTest.md`，把手工验收说明同步到新的暂停/重启/诊断详情/自动重试语义。

### 测试命令与结果

- 命令：`npm --prefix .\frontend run test:unit -- src/modules/stocks/NewsArchiveTab.spec.js`
- 结果：通过，9/9。

- 命令：`dotnet test .\backend\SimplerJiangAiAgent.Api.Tests\SimplerJiangAiAgent.Api.Tests.csproj --filter "FullyQualifiedName~LocalFactAiEnrichmentServiceTests" -v minimal`
- 结果：通过，15/15。

### 尚未完成的验收

- 本次会话没有继续做 browser 或 live source/package 验收。
- 因为高层验收还没补完，所以任务仍保持 `in-progress`，不直接宣告关闭。