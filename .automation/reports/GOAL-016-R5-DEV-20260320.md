# GOAL-016-R5 DEV REPORT - VERSIONED RELEASE, AUTO-UPDATE, AND PUBLIC DOCS - 2026-03-20

## English
- Scope: finish the next distributable product slice by adding shared version metadata, GitHub Release-based update checks, release automation, and a user-facing README with fresh screenshots.
- Product and packaging actions:
  - Added root `Directory.Build.props` so backend, desktop, and installer share version `0.0.1`.
  - Added desktop `AppUpdateService` to query GitHub Releases, compare tags, download the installer asset, and expose the current version label.
  - Updated `Form1.cs` to show the current version in the window title, check for updates after startup, prompt the user, and launch the downloaded installer for upgrade.
  - Updated `scripts/windows-installer.iss` and `scripts/build-windows-installer.ps1` so installer outputs are versioned and align with the shared build version.
  - Added `.github/workflows/release-windows.yml` so pushing a `v*` tag builds the Windows package, builds the installer, zips the portable package, and publishes both assets to GitHub Releases.
- App-shell and API actions:
  - Added backend `/api/app/version` so both the desktop shell and the frontend can display a single authoritative app version and release URL.
  - Updated `frontend/src/App.vue` to fetch and display the current version badge while keeping onboarding routing intact.
  - Kept `AdminLlmSettings` save success wired to emit `settings-saved`, so onboarding state refresh still clears the first-run banner after key setup.
- Documentation actions:
  - Backed up the previous developer-oriented root README to `README.llm.md`.
  - Replaced the root `README.md` with a simpler user-facing product overview covering features, install flow, first-run setup, auto-update behavior, and future goals.
  - Added `frontend/scripts/capture-readme-screenshots.mjs` and generated three fresh `1920x1080` screenshots under `docs/screenshots`.
- Runtime fixes discovered during validation:
  - Fixed another DI ambiguity at startup by changing `ISourceGovernanceReadService` registration to an explicit factory using `AppRuntimePaths`.
  - Added a regression test to ensure `SourceGovernanceReadService` resolves through the container without constructor ambiguity.
- Test commands:
  - `dotnet build .\desktop\SimplerJiangAiAgent.Desktop\SimplerJiangAiAgent.Desktop.csproj`
  - `dotnet build .\backend\SimplerJiangAiAgent.Api\SimplerJiangAiAgent.Api.csproj`
  - `dotnet test .\backend\SimplerJiangAiAgent.Api.Tests\SimplerJiangAiAgent.Api.Tests.csproj --filter "SourceGovernanceReadServiceTests|JsonFileLlmSettingsStoreTests"`
  - `npm --prefix .\frontend run test:unit -- src\__tests__\App.spec.js src\modules\admin\AdminLlmSettings.spec.js`
  - `Invoke-WebRequest http://localhost:5131/api/health`
  - `Invoke-WebRequest http://localhost:5131/api/app/version`
  - `node .\frontend\scripts\capture-readme-screenshots.mjs`
  - `Add-Type -AssemblyName System.Drawing; ...` to verify screenshot dimensions
  - `.\scripts\publish-windows-package.ps1 -Configuration Release -RuntimeIdentifier win-x64 -SelfContained:$false`
  - `.\scripts\build-windows-installer.ps1 -SkipPackagePublish -SelfContained:$false`
- Results:
  - Desktop build passed.
  - Backend build passed.
  - Backend DI regression tests passed: `9/9`.
  - Focused frontend tests passed: `8/8`.
  - Isolated runtime smoke returned `{"status":"ok"}` from `/api/health` and version payload `{"version":"0.0.1", ...}` from `/api/app/version`.
  - Screenshot script produced three valid `1920x1080` PNG assets.
  - Packaging succeeded and installer compilation succeeded, producing `artifacts\installer\SimplerJiangAiAgent-Setup-0.0.1.exe`.
- Issues:
  - A stale packaged desktop/backend process held locks in `artifacts\windows-package` and blocked cleanup during republish. The processes were identified and terminated, then packaging and installer steps were rerun sequentially.
  - A clean backend startup exposed a second constructor ambiguity in `SourceGovernanceReadService`; this was fixed in the same task before final smoke validation.
  - GitHub Release publication is automated but not executed locally yet; a pushed commit plus tag such as `v0.0.1` is still required to trigger the workflow remotely.

## 中文
- 范围：完成下一段可分发产品化收口，包括统一版本号、基于 GitHub Releases 的更新检测、发布自动化，以及面向普通用户的 README 和新截图。
- 产品与打包动作：
  - 新增根级 `Directory.Build.props`，统一后端、桌面端和安装器使用同一版本号 `0.0.1`。
  - 新增桌面端 `AppUpdateService`，负责查询 GitHub Releases、比较版本标签、下载安装器资产，并暴露当前版本号文案。
  - 更新 `Form1.cs`：窗口标题显示当前版本，启动后自动检查更新，弹窗询问用户，并可直接拉起下载后的安装器完成升级。
  - 更新 `scripts/windows-installer.iss` 与 `scripts/build-windows-installer.ps1`，让安装器输出名带版本号，并与共享构建版本保持一致。
  - 新增 `.github/workflows/release-windows.yml`，后续只要推送 `v*` tag，就会自动构建 Windows 可运行目录、编译安装器、打包 portable zip，并上传到 GitHub Releases。
- App 壳与接口动作：
  - 后端新增 `/api/app/version`，让桌面端和前端都从同一来源读取当前版本、仓库地址和最新 Release 地址。
  - 更新 `frontend/src/App.vue`，在保持首启引导逻辑不变的前提下显示版本徽标。
  - 保留 `AdminLlmSettings` 保存成功后发出 `settings-saved` 事件，这样首次配置 key 后首页横幅能即时消失。
- 文档动作：
  - 将原来的开发者向根 README 备份为 `README.llm.md`。
  - 将新的根 `README.md` 改写为普通用户可读版本，覆盖功能、安装方式、首次使用、自动更新和未来目标。
  - 新增 `frontend/scripts/capture-readme-screenshots.mjs`，并在 `docs/screenshots` 下生成 3 张新的 `1920x1080` 截图。
- 验证过程中顺手修复的运行时问题：
  - 干净实例启动时再次暴露 `SourceGovernanceReadService` 的双构造函数 DI 歧义，已改为使用 `AppRuntimePaths` 的显式工厂注册。
  - 新增回归测试，确保 `SourceGovernanceReadService` 可以通过容器稳定解析，不再因为构造函数歧义而在运行时崩溃。
- 测试命令：
  - `dotnet build .\desktop\SimplerJiangAiAgent.Desktop\SimplerJiangAiAgent.Desktop.csproj`
  - `dotnet build .\backend\SimplerJiangAiAgent.Api\SimplerJiangAiAgent.Api.csproj`
  - `dotnet test .\backend\SimplerJiangAiAgent.Api.Tests\SimplerJiangAiAgent.Api.Tests.csproj --filter "SourceGovernanceReadServiceTests|JsonFileLlmSettingsStoreTests"`
  - `npm --prefix .\frontend run test:unit -- src\__tests__\App.spec.js src\modules\admin\AdminLlmSettings.spec.js`
  - `Invoke-WebRequest http://localhost:5131/api/health`
  - `Invoke-WebRequest http://localhost:5131/api/app/version`
  - `node .\frontend\scripts\capture-readme-screenshots.mjs`
  - 使用 `System.Drawing` 校验截图尺寸
  - `.\scripts\publish-windows-package.ps1 -Configuration Release -RuntimeIdentifier win-x64 -SelfContained:$false`
  - `.\scripts\build-windows-installer.ps1 -SkipPackagePublish -SelfContained:$false`
- 结果：
  - 桌面端构建通过。
  - 后端构建通过。
  - 后端 DI 定向回归测试通过：`9/9`。
  - 前端定向单测通过：`8/8`。
  - 隔离端口下运行时烟测返回 `/api/health = {"status":"ok"}`，`/api/app/version = {"version":"0.0.1", ...}`。
  - 截图脚本成功生成 3 张合法的 `1920x1080` PNG。
  - 打包成功，安装器编译成功，产物为 `artifacts\installer\SimplerJiangAiAgent-Setup-0.0.1.exe`。
- 问题：
  - `artifacts\windows-package` 中存在旧的发布版桌面进程和后端进程，占用了文件，导致重新打包清理失败。已定位并停止相关进程，随后按顺序重新执行打包和安装器编译。
  - 干净后端实例启动时暴露出 `SourceGovernanceReadService` 的第二处构造函数歧义，已在同一任务内修复后再做最终烟测。
  - GitHub Release 的发布流程已经自动化，但本地尚未真正触发；后续仍需把本次改动推送到远端并推送类似 `v0.0.1` 的 tag，GitHub Actions 才会实际发布版本。