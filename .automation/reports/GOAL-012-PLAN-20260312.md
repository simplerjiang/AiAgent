# GOAL-012 Planning Report (EN + ZH)

## EN
### Summary
Planned Step 1 of GOAL-012 only: refactor the stock info page into a decoupled terminal workspace without entering GOAL-013 backend scope.

### Planned Scope
- Introduce a dedicated `TerminalView` area for quote widgets, message tape, K-line, minute chart, volume, and MA overlays.
- Introduce a dedicated `CopilotPanel` sidebar/drawer for news impact, multi-agent results, and stock chat.
- Keep AI requests user-triggered only; do not expand into Step 2 local data hub work in this change.
- Add frontend unit coverage for the new layout and focus-mode collapse behavior.
- Sync README/tasks/state/report/directive artifacts after implementation.

### Planned Validation
1. `npm --prefix frontend run test:unit -- src/modules/stocks/StockInfoTab.spec.js`
2. `npm --prefix frontend run build`
3. Edge MCP check if a runnable frontend target is available in this session.

### Risks Noted
- `StockInfoTab.vue` is a large mixed-responsibility component, so layout-only refactoring must avoid breaking existing fetch/chat/history behavior.
- No backend changes are in scope for this step.

## ZH
### 摘要
本轮只执行 GOAL-012 的 Step 1：把股票信息页拆成终端主视区与 AI 侧栏，不进入 GOAL-013 的后端本地数据中枢范围。

### 规划范围
- 新增独立 `TerminalView` 区域，承载行情挂件、消息带、K 线、分时、成交量与均线。
- 新增独立 `CopilotPanel` 侧栏/抽屉，承载资讯影响、多 Agent 结果与股票聊天。
- 保持 AI 请求仍为用户主动触发，本次不推进 Step 2 的本地数据中枢。
- 为新布局与专注模式折叠行为补充前端单测。
- 开发完成后同步 README/tasks/state/report/directive 等自动化产物。

### 计划校验
1. `npm --prefix frontend run test:unit -- src/modules/stocks/StockInfoTab.spec.js`
2. `npm --prefix frontend run build`
3. 若本轮会话内存在可运行前端目标，则补做 Edge MCP 页面检查。

### 已知风险
- `StockInfoTab.vue` 体积较大、职责混合，布局重构时需要避免影响现有查询、聊天、历史记录逻辑。
- 本步骤不涉及任何后端改动。
