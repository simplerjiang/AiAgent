# MANUAL-20260406-BUGFIX-R1-20260406

## EN

### Scope

- Round 1 for the 2026-04-05 manually logged open bugs.
- This round only formalizes the current bug ledger and implements Bug 1.
- Bugs 2-8 remain open and were intentionally left untouched in this change.

### Plan

- Move the freeform 2026-04-05 bullet list in `.automation/buglist.md` into formal current-open bug entries with ordered bug IDs and acceptance notes.
- Fix the archive pending-clean endpoint at the root cause: cover market, sector, and stock local-fact rows instead of only market rows.
- Keep backend batching bounded, but return truthful processed/remaining counts so the frontend can continue until the backlog is actually drained or a clear stop condition is reached.
- Add targeted regression coverage for the backend enrichment sweep and the frontend archive button flow.

### Actions

- Updated `.automation/buglist.md` so `当前开放项` now contains eight structured entries for the 2026-04-05 manual bugs, with severity, status, reproduction summaries, and acceptance notes.
- Updated `.automation/tasks.json` with task `MANUAL-20260406-BUGFIX-R1` to track this manual bug-fix slice.
- Updated `backend/SimplerJiangAiAgent.Api/Infrastructure/Jobs/LocalFactAiEnrichmentService.cs`:
  - kept the existing market-only enrichment path for ingestion flows,
  - added a dedicated archive pending sweep that loads bounded pending rows from market, sector, and stock scopes,
  - added truthful `processed` and `remaining` counts for each scope,
  - added an explicit stop reason when a sweep makes no progress.
- Follow-up corrected `backend/SimplerJiangAiAgent.Api/Modules/Stocks/StocksModule.cs` so `/api/news/archive/process-pending` now forwards `CancellationToken` into `ProcessPendingBatchAsync(...)` and returns the pending-sweep summary instead of the stale `{ success: true }` placeholder payload.
- Updated `frontend/src/modules/stocks/NewsArchiveTab.vue` so the archive page keeps calling the endpoint until pending cleaning is actually complete or a clear stop condition is reached, and only then shows the correct final toast.
- Updated `backend/SimplerJiangAiAgent.Api.Tests/LocalFactAiEnrichmentServiceTests.cs` with a regression that proves one sweep can leave truthful remaining counts and a second sweep drains the rest.
- Updated `frontend/src/modules/stocks/NewsArchiveTab.spec.js` with regressions for repeated archive sweeps and honest partial-stop messaging.

### Test Commands And Results

- Command: `dotnet test .\backend\SimplerJiangAiAgent.Api.Tests\SimplerJiangAiAgent.Api.Tests.csproj --filter "FullyQualifiedName~LocalFactAiEnrichmentServiceTests"`
- Result: passed.

- Command: `npm --prefix .\frontend run test:unit -- src/modules/stocks/NewsArchiveTab.spec.js`
- Result: passed.

- Command: `dotnet build backend/SimplerJiangAiAgent.Api/SimplerJiangAiAgent.Api.csproj`
- Result: passed, with 2 existing nullable warnings in `SourceGovernanceService.cs` unrelated to this route fix.

- Command: `dotnet run --project .\backend\SimplerJiangAiAgent.Api\SimplerJiangAiAgent.Api.csproj` and `Invoke-WebRequest -Uri 'http://localhost:5127/api/news/archive/process-pending' -Method Post -UseBasicParsing -TimeoutSec 20`
- Result: fresh source live probe did not complete cleanly. During the validation window, the source-run process kept logging unrelated `LocalFactIngestionService` upstream SSL EOF failures and never exposed a reachable listener on the probe port, so the endpoint body could not be re-verified over HTTP in this session.

### Outcome

- Bug 1 source changes now include the missed route wiring: archive sweeps are serialized server-side, `/api/news/archive/process-pending` returns the same `processed / remaining / completed / stopReason` semantics consumed by the new frontend loop, and the UI no longer treats `processed.total == 0` as a generic completion signal.
- The earlier targeted backend and frontend unit tests did not cover this route wiring path, and the fresh HTTP probe stayed blocked by unrelated startup behavior, so Bug 1 remains in `ready for fresh Test Agent / User Representative validation` state rather than accepted or closed.

## ZH

### 范围

- 本轮是 2026-04-05 手工补录开放问题的第 1 轮修复。
- 本轮只做两件事：把 buglist 结构化，以及落 Bug 1。
- Bug 2-8 保持开放，故意不在本轮处理。

### 计划

- 把 `.automation/buglist.md` 中 2026-04-05 的自由文本新 Bug 列表整理成正式的 `当前开放项` 条目，补齐有序 Bug ID、严重级别、状态、复现摘要和验收说明。
- 从根因修复全量资讯库的待清洗入口：不能再只清 market，也不能再单批后直接谎报“完成”。
- 后端继续保留安全批处理边界，但必须返回真实的 processed/remaining 计数；前端则持续调用直到真正清空 backlog，或出现明确停止条件。
- 为后端 sweep 逻辑和前端按钮交互补最接近的回归测试。

### 本轮动作

- 更新 `.automation/buglist.md`，把 2026-04-05 手工补录的 8 个问题全部整理进 `当前开放项`，并保留原有历史说明为 `历史备注`。
- 更新 `.automation/tasks.json`，新增任务 `MANUAL-20260406-BUGFIX-R1` 记录这一轮手动 Bug 修复。
- 修改 `backend/SimplerJiangAiAgent.Api/Infrastructure/Jobs/LocalFactAiEnrichmentService.cs`：
  - 保留原有给 ingestion 流程使用的 market-only 清洗入口，避免放大既有链路影响面；
  - 新增给资讯库按钮使用的 pending sweep，单轮同时覆盖 market、sector、stock 三类待清洗记录；
  - 返回每个层级真实的 `processed` / `remaining` 计数；
  - 当一轮清洗没有任何进展时返回明确 `stopReason`。
- follow-up 修正 `backend/SimplerJiangAiAgent.Api/Modules/Stocks/StocksModule.cs` 中遗漏的 route wiring，让 `/api/news/archive/process-pending` 携带 `CancellationToken` 调用 `ProcessPendingBatchAsync(...)`，并返回真实 sweep summary，而不是旧的 `{ success: true }` 占位响应。
- 修改 `frontend/src/modules/stocks/NewsArchiveTab.vue`，让“批量清洗待处理”按钮持续请求后端，直到全部清完或命中清晰停止条件；只有真正 `remaining = 0` 才提示“完成”。
- 修改 `backend/SimplerJiangAiAgent.Api.Tests/LocalFactAiEnrichmentServiceTests.cs`，新增回归锁定“第一轮仍有剩余、第二轮清空”的行为。
- 修改 `frontend/src/modules/stocks/NewsArchiveTab.spec.js`，新增回归锁定“持续多轮请求直到完成”和“无进展时必须 warning，不得 success”的行为。

### 测试命令与结果

- 命令：`dotnet test .\backend\SimplerJiangAiAgent.Api.Tests\SimplerJiangAiAgent.Api.Tests.csproj --filter "FullyQualifiedName~LocalFactAiEnrichmentServiceTests"`
- 结果：通过。

- 命令：`npm --prefix .\frontend run test:unit -- src/modules/stocks/NewsArchiveTab.spec.js`
- 结果：通过。

- 命令：`dotnet build backend/SimplerJiangAiAgent.Api/SimplerJiangAiAgent.Api.csproj`
- 结果：通过；存在 2 个 `SourceGovernanceService.cs` 的既有 nullable warning，与本次 route fix 无关。

- 命令：`dotnet run --project .\backend\SimplerJiangAiAgent.Api\SimplerJiangAiAgent.Api.csproj` 与 `Invoke-WebRequest -Uri 'http://localhost:5127/api/news/archive/process-pending' -Method Post -UseBasicParsing -TimeoutSec 20`
- 结果：fresh source live probe 未能干净完成。验证窗口内，source-run 进程持续输出与本次修改无关的 `LocalFactIngestionService` 上游 SSL EOF 失败日志，且未暴露可访问的本地监听端口，因此本次会话中无法通过 HTTP 重新确认 endpoint body。

### 结果

- Bug 1 的源码修复现已补齐此前遗漏的 route wiring：后端 archive sweep 已做单进程串行保护，`/api/news/archive/process-pending` 现在返回前端循环所需的 `processed / remaining / completed / stopReason` 语义，前端也不再把 `processed.total == 0` 当成通用完成条件。
- 之前的后端与前端定向单测并未覆盖这个 route wiring 路径，且 fresh HTTP probe 又被无关启动行为阻塞，因此 Bug 1 当前仍处于“源码已修复，待 Test Agent / User Representative Agent 基于 fresh build 继续验证”状态，尚未视为验收关闭。
- Bug 2-8 继续保留在 buglist 中，等待后续轮次处理。