# ISSUE-20260310 Prompt Hardening Report

## Planning Report (EN)
- Task source: `20260310_ISSUE.txt` with three core concerns (news pollution, own news library, white-box MCP/Skill + scheduled task execution).
- Planning output created in `.automation/plan/ISSUE-20260310.md` with 6 concrete steps.
- Scope decision: this run focuses on automation prompt hardening and task governance artifacts; no backend/frontend feature code changed in this step.
- Key risk: requirements can stay abstract if prompts are not explicit enough.
- Mitigation: encode mandatory constraints directly in planner/developer/tester prompts.

## 规划报告（ZH）
- 任务来源：`20260310_ISSUE.txt` 的三个核心问题（新闻污染、自建新闻库、白盒 MCP/Skill + 定时任务执行）。
- 已在 `.automation/plan/ISSUE-20260310.md` 形成 6 步可执行计划。
- 范围决策：本轮先完成自动化提示词与任务治理增强，不改后端/前端业务代码。
- 主要风险：需求停留在描述层，无法稳定落地。
- 应对：将关键约束直接写入 planner/developer/tester 提示词。

## Development Report (EN)
- Updated `.automation/prompts/planner.md`:
  - Added anti-pollution planning policy (trusted-source first, rumor downgrade, missing timestamp downgrade).
  - Added mandatory design items for market/sector/stock news library and scheduled collection.
  - Added white-box MCP/Skill planning requirements (contracts, permissions, observability, unfinished-task loop).
- Updated `.automation/prompts/developer.md`:
  - Added implementation guardrails for source allowlist/blocklist and deterministic neutral fallback.
  - Added persistence/scheduler/queryability requirements for news knowledge base.
  - Added white-box capability registration and task queue observability requirements.
- Updated `.automation/prompts/tester.md`:
  - Added targeted validation checks for anti-pollution behavior, news ingestion scheduling/persistence, and MCP/Skill permission boundaries + audit logs.
- Synced governance artifacts:
  - Added task `ISSUE-20260310` to `.automation/tasks.json`.
  - Synced `.automation/state.json` to current task/report.
  - Added synced target checklist item in `README.md`.
  - Added one new continuous rule in `.github/copilot-instructions.md` for mandatory source-tier policy in news prompts.

## Task Decomposition Deepening (EN)
- Added executable phase plans:
  - `.automation/plan/ISSUE-20260310.md` upgraded to a full phased blueprint (P1/P2/P3) with scope, steps, DoD, tests, and rollback.
  - `.automation/plan/ISSUE-20260310-P1.md` anti-pollution implementation detail.
  - `.automation/plan/ISSUE-20260310-P2.md` news knowledge base + scheduler implementation detail.
  - `.automation/plan/ISSUE-20260310-P3.md` white-box MCP/Skill runtime implementation detail.
- Split execution queue in `.automation/tasks.json`:
  - `ISSUE-20260310-P1` (anti-pollution guardrails)
  - `ISSUE-20260310-P2` (news DB + scheduled ingestion)
  - `ISSUE-20260310-P3` (white-box MCP/Skill + unfinished-task loop)
- Synced roadmap checklist in `README.md` with the three new phase tasks.
- Enriched `.automation/plan/ISSUE-20260310-P1.md` with code-level blueprint, planned touchpoints, output contract, boundary tests, and milestone deliverables.

## P0 Decision Update (EN)
- New priority task added: `ISSUE-20260310-P0` (dynamic source governance baseline).
- Design decision: adopt Native-AI hybrid governance where LLM discovers candidate sources daily, but programmatic verification controls admission to production.
- Added dedicated plan `.automation/plan/ISSUE-20260310-P0.md` with source registry, health scoring, auto promotion/quarantine, and minimal-manual workflow.
- Upgraded `.automation/plan/ISSUE-20260310-P2.md` to explicitly depend on P0 and consume only verified source tiers.
- Synced `README.md`, `.automation/tasks.json`, and `.automation/state.json` for P0-first execution order.

## Native-AI Crawler Autonomy Update (EN)
- User requirement confirmed: allow LLM to automatically add crawl source URLs, generate crawl workflows, run daily health checks, and auto-update crawler code when parsing breaks.
- Updated `.automation/plan/ISSUE-20260310-P0.md` with an autonomous maintainer pipeline:
  - LLM source discovery -> programmatic verification
  - LLM crawler patch generation -> sandbox validation -> auto deployment -> health-based auto rollback
- Added strict safety guardrails for autonomous code updates:
  - File-scope allowlist for parser/test files only
  - Mandatory compile + targeted tests + replay validation before deployment
  - Traceable change records with traceId and rollback points
- Updated `ISSUE-20260310-P0` notes and `README.md` checklist to reflect full autonomous crawler lifecycle.

## P0 Implementation Progress (EN)
- Implemented P0 phase-1 backend scaffold:
  - Added source governance entities in backend data model:
    - `NewsSourceRegistry`, `NewsSourceHealthDaily`, `NewsSourceCandidate`
  - Wired EF Core context mappings and indexes in `AppDbContext`.
  - Added automated governance runtime:
    - `SourceGovernanceOptions`
    - `SourceGovernancePolicy`
    - `SourceGovernanceService` (`ISourceGovernanceService`)
    - `SourceGovernanceWorker`
  - Registered options/services/worker in `Program.cs` and configuration in `appsettings*.json`.
  - Added deterministic unit tests: `SourceGovernancePolicyTests`.
- Local database schema verification and fix:
  - Verified missing tables with `sqlcmd`.
  - Applied SQL DDL locally to create `NewsSourceRegistries`, `NewsSourceHealthDailies`, `NewsSourceCandidates` with indexes/FK.
  - Re-verified all three tables exist.

## P0 Completion Update (EN)
- Completed full P0 workflow implementation (not only scaffold):
  - Added new entities/tables for full pipeline:
    - `NewsSourceVerificationRun`
    - `CrawlerChangeQueue`
    - `CrawlerChangeRun`
  - Expanded `SourceGovernanceService` to execute full chain:
    - `discover-sources` (LLM)
    - `verify-candidates` (HTTP + heuristic scoring)
    - `promote-or-quarantine` (policy)
    - `auto-fix-crawlers` (LLM patch proposal)
    - `validate-crawler-patches` (path safety allowlist)
    - `deploy-crawler-patches` (state transition + run log)
  - Added `SourceGovernanceSchemaInitializer` and startup integration to auto-heal missing P0 tables on existing databases.
  - Added integration-style tests in `SourceGovernanceServiceTests`.
- Blocking-risk check for P1/P2/P3:
  - P0 data model, worker runtime, schema, and tests are in place.
  - P1/P2/P3 can proceed without missing-source-governance dependency blockers.

## Dual-Track Data Architecture Update (EN)
- User clarified two analysis tracks are required:
  - Stock fact track: company events, disclosures, themes, dividends/financing, financial analysis.
  - Sector intelligence track: sector-level context and rotation signals.
- Updated `.automation/plan/ISSUE-20260310-P2.md` to formalize dual-track ingestion and context assembly.
- Added practical sector-source strategy:
  - authoritative/preferred sources first; fallback sources only as supplemental evidence.
  - dynamic source governance via `source_registry` for unstable sites.
- Added fallback method when sector sources are weak:
  - derive sector signals from local market microstructure (breadth, limit-up ratio, turnover expansion, leader-follow diffusion),
  - then fuse with limited high-trust news and downgrade to neutral when evidence remains insufficient.
- Updated `ISSUE-20260310-P2` task notes to reflect dual-track delivery and fallback behavior.

## 开发报告（ZH）
- 已更新 `.automation/prompts/planner.md`：
  - 新增抗污染规划约束（可信源优先、谣言降级、缺失时间戳降级）。
  - 新增市场/板块/个股新闻库与定时采集的必填设计项。
  - 新增白盒 MCP/Skill 规划要求（契约、权限、可观测性、未完成任务循环）。
- 已更新 `.automation/prompts/developer.md`：
  - 新增来源白名单/黑名单与中性回退的实现约束。
  - 新增新闻知识库落库、调度与可查询性要求。
  - 新增能力注册与任务队列可观测要求。
- 已更新 `.automation/prompts/tester.md`：
  - 新增抗污染、定时采集落库、MCP/Skill 权限与审计日志校验项。
- 已同步治理文件：
  - `.automation/tasks.json` 新增 `ISSUE-20260310`。
  - `.automation/state.json` 同步到当前任务与报告路径。
  - `README.md` 同步新增目标清单条目。
  - `.github/copilot-instructions.md` 新增一条持续规则（新闻提示词必须显式来源分级策略）。

## 任务拆分深化（ZH）
- 已新增可执行阶段计划：
  - `.automation/plan/ISSUE-20260310.md` 升级为完整分阶段蓝图（P1/P2/P3），包含范围、步骤、DoD、测试与回滚。
  - `.automation/plan/ISSUE-20260310-P1.md`：新闻抗污染实施细化。
  - `.automation/plan/ISSUE-20260310-P2.md`：新闻库与定时采集实施细化。
  - `.automation/plan/ISSUE-20260310-P3.md`：白盒 MCP/Skill 与任务运行时实施细化。
- 已在 `.automation/tasks.json` 中拆分执行队列：
  - `ISSUE-20260310-P1`（抗污染守护）
  - `ISSUE-20260310-P2`（新闻库 + 定时采集）
  - `ISSUE-20260310-P3`（白盒能力 + 未完成任务循环）
- `README.md` 已同步三项分阶段任务，方便按阶段推进与验收。
- 已进一步增强 `.automation/plan/ISSUE-20260310-P1.md`：补充代码级落地蓝图、改动落点、输出契约、边界测试用例与里程碑交付项。

## P0 决策更新（ZH）
- 新增最高优先级任务：`ISSUE-20260310-P0`（动态来源治理基座）。
- 设计决策：采用 Native-AI 混合治理，LLM 每日自动发现候选源，但是否进入生产链路由程序化验证控制。
- 已新增计划 `.automation/plan/ISSUE-20260310-P0.md`，包含来源注册表、健康评分、自动升降级/隔离与低人工干预流程。
- 已升级 `.automation/plan/ISSUE-20260310-P2.md`，明确依赖 P0，只消费已验证来源等级。
- 已同步 `README.md`、`.automation/tasks.json`、`.automation/state.json`，执行顺序调整为 P0 优先。

## Native-AI 爬虫自治更新（ZH）
- 已确认你的要求：允许 LLM 自动新增爬取地址、生成爬取流程、每日校验健康状态，并在爬虫失效时自动更新代码。
- 已更新 `.automation/plan/ISSUE-20260310-P0.md`，新增自治维护流水线：
  - LLM 发现来源 -> 程序化准入验证
  - LLM 生成爬虫补丁 -> 沙箱验证 -> 自动发布 -> 健康回滚
- 已加入自动改码安全闸门：
  - 仅允许 parser/测试文件白名单目录改动
  - 发布前必须编译通过 + 定向单测 + 历史样本回放
  - 每次自动改动记录 traceId 与回滚点，保证可审计
- 已同步 `ISSUE-20260310-P0` 任务说明与 `README.md` 清单描述，确保执行方向一致。

## 双轨数据架构更新（ZH）
- 已按你的要求明确两类分析信息：
  - 个股事实轨：公司大事、公告、核心题材、分红融资、财务分析。
  - 板块情报轨：板块层面的政策/产业/资金/轮动信息。
- 已更新 `.automation/plan/ISSUE-20260310-P2.md`，将双轨采集、双轨入库、双轨上下文注入写为正式方案。
- 已补充板块来源策略：优先权威/主流来源，不稳定来源由 `source_registry` 动态治理。
- 已补充“板块优质来源不足”时的替代方案：
  - 由本地量价扩散信号反推板块强度，
  - 再与少量高可信新闻融合，证据不足时自动降级为中性。
- 已同步 `ISSUE-20260310-P2` 任务说明，确保开发目标可执行。

## P0 实现进度（ZH）
- 已完成 P0 第一批后端代码骨架：
  - 新增来源治理实体：
    - `NewsSourceRegistry`、`NewsSourceHealthDaily`、`NewsSourceCandidate`
  - 在 `AppDbContext` 接入 DbSet、索引与关联。
  - 新增自治治理运行时：
    - `SourceGovernanceOptions`
    - `SourceGovernancePolicy`
    - `SourceGovernanceService`（含 `ISourceGovernanceService`）
    - `SourceGovernanceWorker`
  - 在 `Program.cs` 和 `appsettings*.json` 完成注册与配置接入。
  - 新增确定性单测 `SourceGovernancePolicyTests`。
- 本地数据库结构校验与修复：
  - 使用 `sqlcmd` 检查发现新表缺失。
  - 已本地执行 DDL 创建 `NewsSourceRegistries`、`NewsSourceHealthDailies`、`NewsSourceCandidates` 及索引/外键。
  - 已再次校验三张表均存在。

  ## P0 完成更新（ZH）
  - 已完成 P0 全链路实现（不再只是骨架）：
    - 新增完整流水线实体/表：
      - `NewsSourceVerificationRun`
      - `CrawlerChangeQueue`
      - `CrawlerChangeRun`
    - 扩展 `SourceGovernanceService` 为完整链路：
      - `discover-sources`（LLM）
      - `verify-candidates`（HTTP校验 + 评分）
      - `promote-or-quarantine`（策略判定）
      - `auto-fix-crawlers`（LLM补丁提案）
      - `validate-crawler-patches`（路径安全白名单）
      - `deploy-crawler-patches`（状态发布 + 运行记录）
    - 新增 `SourceGovernanceSchemaInitializer` 并接入启动流程，支持旧库自动补齐 P0 表结构。
    - 新增 `SourceGovernanceServiceTests` 集成测试。
  - 对 P1/P2/P3 的阻塞风险检查：
    - P0 的数据模型、运行任务、数据库结构与测试基线均已就位。
    - 后续阶段不再受“来源治理缺失”阻塞。

## Test Commands & Results (EN + ZH)
- Command: `pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; Get-Content '.automation/tasks.json' | ConvertFrom-Json | Out-Null; Get-Content '.automation/state.json' | ConvertFrom-Json | Out-Null; Select-String -Path '.automation/prompts/planner.md' -Pattern 'Trusted-source first' | Out-Null; Select-String -Path '.automation/prompts/developer.md' -Pattern 'allowlist/blocklist' | Out-Null; Select-String -Path '.automation/prompts/tester.md' -Pattern 'News anti-pollution' | Out-Null; Write-Output 'VALIDATION_OK'"`
- Result: Passed (`VALIDATION_OK`).
- 结论：校验通过（`VALIDATION_OK`）。
- Command: `pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; $tasks = Get-Content '.automation/tasks.json' | ConvertFrom-Json; ($tasks.tasks | Where-Object { $_.id -eq 'ISSUE-20260310-P1' }).id | Out-Null; ($tasks.tasks | Where-Object { $_.id -eq 'ISSUE-20260310-P2' }).id | Out-Null; ($tasks.tasks | Where-Object { $_.id -eq 'ISSUE-20260310-P3' }).id | Out-Null; Test-Path '.automation/plan/ISSUE-20260310-P1.md' | Where-Object { $_ -eq $true } | Out-Null; Test-Path '.automation/plan/ISSUE-20260310-P2.md' | Where-Object { $_ -eq $true } | Out-Null; Test-Path '.automation/plan/ISSUE-20260310-P3.md' | Where-Object { $_ -eq $true } | Out-Null; Select-String -Path 'README.md' -Pattern 'ISSUE-20260310-P1|ISSUE-20260310-P2|ISSUE-20260310-P3' | Out-Null; Write-Output 'BREAKDOWN_VALIDATION_OK'"`
- Result: Passed (`BREAKDOWN_VALIDATION_OK`).
- 结论：任务拆分与文档同步校验通过（`BREAKDOWN_VALIDATION_OK`）。
- Command: `pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; Select-String -Path '.automation/plan/ISSUE-20260310-P1.md' -Pattern 'Implementation Blueprint|Planned Code Touchpoints|Test Strategy|Boundary Test Cases|Milestone & Deliverables' | Out-Null; Write-Output 'P1_DOC_ENRICH_OK'"`
- Result: Passed (`P1_DOC_ENRICH_OK`).
- 结论：P1 计划文档深化内容已写入并通过校验（`P1_DOC_ENRICH_OK`）。
- Command: `pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; $tasks = Get-Content '.automation/tasks.json' | ConvertFrom-Json; ($tasks.tasks | Where-Object { $_.id -eq 'ISSUE-20260310-P0' }).id | Out-Null; Test-Path '.automation/plan/ISSUE-20260310-P0.md' | Where-Object { $_ -eq $true } | Out-Null; Select-String -Path '.automation/plan/ISSUE-20260310-P2.md' -Pattern 'Dependency|source_registry|quarantine|LLM 候选源发现' | Out-Null; Select-String -Path 'README.md' -Pattern 'ISSUE-20260310-P0' | Out-Null; Select-String -Path '.github/copilot-instructions.md' -Pattern 'dynamic news-source reliability|动态资讯源稳定性' | Out-Null; Write-Output 'P0_NATIVE_AI_PLAN_OK'"`
- Result: Passed (`P0_NATIVE_AI_PLAN_OK`).
- 结论：P0 文档与任务同步校验通过（`P0_NATIVE_AI_PLAN_OK`）。
- Command: `pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; Select-String -Path '.automation/plan/ISSUE-20260310-P0.md' -Pattern 'LLM Crawler Maintainer|Autonomous Crawler Policy|auto-fix-crawlers|自动回滚|Safety Guardrails' | Out-Null; Select-String -Path '.automation/tasks.json' -Pattern '自动新增爬取地址|自动生成代码补丁|自动发布与回滚' | Out-Null; Select-String -Path 'README.md' -Pattern '自动新增爬取地址/流程|爬虫失效自动修复发布' | Out-Null; Write-Output 'P0_AUTONOMY_RULES_OK'"`
- Result: Passed (`P0_AUTONOMY_RULES_OK`).
- 结论：“自治爬虫规则”同步校验通过（`P0_AUTONOMY_RULES_OK`）。
- Command: `pwsh -NoProfile -Command "$ErrorActionPreference='Stop'; Select-String -Path '.automation/plan/ISSUE-20260310-P2.md' -Pattern 'Dual-Track Information Model|stock_facts|sector_signals|Sector Source Strategy|If Good Sector Sources Are Limited' | Out-Null; Select-String -Path '.automation/tasks.json' -Pattern '双轨数据体系|板块优质来源不足|本地量价扩散信号' | Out-Null; Write-Output 'P2_DUAL_TRACK_OK'"`
- Result: Passed (`P2_DUAL_TRACK_OK`).
- 结论：P2 双轨架构同步校验通过（`P2_DUAL_TRACK_OK`）。
- Command: `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj --filter SourceGovernancePolicyTests`
- Result: Failed first due locked API process (`SimplerJiangAiAgent.Api.exe` in use), then passed after stopping process. Summary: Total 3, Passed 3, Failed 0.
- 结论：P0 定向单测最终通过（3/3）。
- Command: `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj`
- Result: Passed. Summary: Total 48, Passed 48, Failed 0.
- 结论：后端全量单测通过（48/48）。
- Command: `sqlcmd -S . -d SimplerJiangAiAgent -Q "SET NOCOUNT ON; SELECT name FROM sys.tables WHERE name IN ('NewsSourceRegistries','NewsSourceHealthDailies','NewsSourceCandidates');"`
- Result: Initially no rows; after applying local DDL, returned all three table names.
- 结论：P0 涉及数据库结构本地校验与修复完成。
- Command: `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj --filter SourceGovernance`
- Result: Passed. Summary: Total 5, Passed 5, Failed 0.
- 结论：P0 定向策略+服务全链路测试通过（5/5）。
- Command: `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj`
- Result: Passed. Summary: Total 50, Passed 50, Failed 0.
- 结论：后端全量单测通过（50/50）。
- Command: `sqlcmd -S . -d SimplerJiangAiAgent -Q "SET NOCOUNT ON; SELECT name FROM sys.tables WHERE name IN ('NewsSourceRegistries','NewsSourceHealthDailies','NewsSourceCandidates','NewsSourceVerificationRuns','CrawlerChangeQueues','CrawlerChangeRuns') ORDER BY name;"`
- Result: Passed; all 6 P0 tables returned.
- 结论：P0 数据库结构完整性校验通过（6/6）。

## Edge MCP Check Decision
- Not required in this run because no frontend/UI code changed.
- 本次未改前端/UI 代码，按规则无需执行 Edge MCP UI 校验。

## Ports / Runtime Notes
- No new services started in this run; no new port allocation needed.
- 本次未启动新服务进程，无新增端口占用。

## P0 Final Development Closure (EN)
- Enhanced `SourceGovernanceService` patch lifecycle from state-only transition to executable validation:
  - Added `ICommandRunner` + `ProcessCommandRunner` and registered in `Program.cs`.
  - `ValidateCrawlerPatchProposalsAsync` now enforces:
    - `dotnet build backend/SimplerJiangAiAgent.Api/SimplerJiangAiAgent.Api.csproj -nologo`
    - non-empty proposal `testCommand`
    - non-zero exit code rejection (`build_failed` / `test_failed` / `missing_test_command`).
- Implemented automatic rollback stage:
  - Added `CrawlerChangeStatus.RolledBack`.
  - Added `EvaluateRollbackForDeployedChangesAsync` to rollback deployed changes when source health degrades (quarantine or consecutive failures).
  - Added rollback observation window via `SourceGovernanceOptions.RollbackGraceMinutes` (default 1440) to prevent same-cycle immediate rollback.
- Expanded tests in `SourceGovernanceServiceTests`:
  - command-validation fail path rejects queue,
  - deployed queue rollback path verifies `rolled_back` run records,
  - updated existing deployment test to align with grace window.

## P0 最终开发收口（ZH）
- 将 `SourceGovernanceService` 的补丁链路从“仅状态流转”升级为“可执行校验”：
  - 新增 `ICommandRunner` + `ProcessCommandRunner`，并在 `Program.cs` 注入。
  - `ValidateCrawlerPatchProposalsAsync` 现强制执行：
    - `dotnet build backend/SimplerJiangAiAgent.Api/SimplerJiangAiAgent.Api.csproj -nologo`
    - 提案必须包含 `testCommand`
    - 命令非 0 直接拒绝（`build_failed` / `test_failed` / `missing_test_command`）。
- 新增自动回滚阶段：
  - 增加 `CrawlerChangeStatus.RolledBack`。
  - 增加 `EvaluateRollbackForDeployedChangesAsync`，当来源健康退化（隔离或连续失败）时自动回滚。
  - 增加 `SourceGovernanceOptions.RollbackGraceMinutes`（默认 1440 分钟）作为观察窗口，避免同一轮刚发布立即回滚。
- 扩展 `SourceGovernanceServiceTests`：
  - 覆盖命令校验失败拒绝路径，
  - 覆盖已发布队列自动回滚与 `rolled_back` 运行记录，
  - 同步更新既有发布测试断言以匹配观察窗口策略。

## Additional Test Commands & Results (EN + ZH)
- Command: `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj --filter SourceGovernance --nologo`
- Result: Passed. Summary: Total 7, Passed 7, Failed 0.
- 结论：来源治理定向测试通过（7/7）。
- Command: `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj --nologo`
- Result: Passed. Summary: Total 52, Passed 52, Failed 0.
- 结论：后端全量测试通过（52/52）。
- Incident handled: build output lock by running API process (`PID 19160`) caused MSB3021/MSB3027; resolved by stopping the process and re-running tests.
- 问题处理：运行中的 API 进程（`PID 19160`）导致构建文件锁（MSB3021/MSB3027）；已停止进程并重跑通过。

## P0 Gap-Closure Completion (EN)
- Closed remaining P0 gaps in `SourceGovernanceService` and related entities:
  - Real patch artifact flow: proposal now carries `patches(path+content)` and `replayCommand`.
  - Sandbox validation now executes full sequence: apply patches -> build -> target tests -> replay -> restore sandbox.
  - Deploy now applies patches to repository files and persists `DeploymentBackupJson` for rollback.
  - Rollback now restores from deployment backup payload (not status-only transition) and writes rollback run records.
  - Path control tightened to parser service + parser test directories only.
  - Verification scoring now includes `duplicateRate`, `contentDepth`, `crossSourceAgreement`.
- Added schema compatibility updates in `SourceGovernanceSchemaInitializer` for new queue/run columns and verification metrics columns.
- Expanded integration tests with sandbox repository root isolation to validate deploy + rollback behavior without touching workspace files.

## P0 缺口收口完成（ZH）
- 已在 `SourceGovernanceService` 与相关实体中补齐剩余 P0 缺口：
  - 补丁提案升级为真实制品：新增 `patches(path+content)` 与 `replayCommand`。
  - 沙箱校验升级为完整链路：应用补丁 -> 编译 -> 定向单测 -> 回放命令 -> 还原沙箱。
  - 发布阶段改为真实应用补丁并持久化 `DeploymentBackupJson` 回滚元数据。
  - 回滚阶段改为按部署备份恢复文件（不再是仅状态改写），并写入回滚运行记录。
  - 路径控制收紧为仅允许 parser 服务目录与 parser 测试目录。
  - 候选源评分补齐 `duplicateRate`、`contentDepth`、`crossSourceAgreement` 三维度。
- `SourceGovernanceSchemaInitializer` 已补充新列兼容逻辑，覆盖队列表与验证记录表新增字段。
- 已扩展集成测试并引入沙箱仓库根目录隔离，验证发布/回滚行为且不污染工作区文件。

## Final Validation Commands & Results (EN + ZH)
- Command: `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj --filter SourceGovernance --nologo`
- Result: Passed. Summary: Total 7, Passed 7, Failed 0.
- 结论：来源治理定向测试通过（7/7）。
- Command: `dotnet test backend/SimplerJiangAiAgent.Api.Tests/SimplerJiangAiAgent.Api.Tests.csproj --nologo`
- Result: Passed. Summary: Total 52, Passed 52, Failed 0.
- 结论：后端全量测试通过（52/52）。
- Command: `sqlcmd -S . -d SimplerJiangAiAgent -Q "...sys.columns check..."`
- Result: Initially missing new columns on existing local DB; applied ALTER patch and re-check passed.
- 结论：本地数据库已补齐 `CrawlerChangeQueues` 新列（`ProposedPatchJson/ProposedReplayCommand/DeploymentBackupJson`）与 `NewsSourceVerificationRuns` 新列（`DuplicateRate/ContentDepth/CrossSourceAgreement`）。