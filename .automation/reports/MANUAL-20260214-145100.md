# Task Report

## Task
- ID: OPS-006 (Planning / Supervisor setup)
- Title: OpenCode supervised launch + E2E
- Timestamp: 20260214-145100

## English (For Agents)
### Summary
- Prepared supervised execution flow for OpenCode executor.
- Added explicit rule requiring supervisor re-test before final user reply.
- Added next task OPS-006 and created dedicated executor prompt file.

### Changes
- Updated `.github/copilot-instructions.md` with supervision gate rule.
- Updated `.automation/tasks.json` by adding `OPS-006`.
- Added `.automation/prompts/opencode-supervisor-OPS-006.md` for executor instructions.

### Validation
- Environment check command run:
  - `if ($env:GLM_API_KEY) { ... } else { ... }`
  - Result: `GLM_API_KEY_MISSING`

### Blocker
- OpenCode cannot be run against GLM-5 yet because `GLM_API_KEY` is not set in current terminal environment.

## 中文（给你看的）
### 摘要
- 已准备好“我监督、OpenCode 执行”的流程。
- 已新增“执行者完成后必须由我复测验收”的规则。
- 已新增下一任务 OPS-006，并写好执行者专用提示词。

### 修改
- 更新 `.github/copilot-instructions.md`：新增监督复测规则。
- 更新 `.automation/tasks.json`：新增 `OPS-006`。
- 新增 `.automation/prompts/opencode-supervisor-OPS-006.md`：执行者指令。

### 校验
- 已执行环境检查命令，结果：`GLM_API_KEY_MISSING`。

### 阻塞
- 当前终端环境未设置 `GLM_API_KEY`，因此还不能在命令行安全驱动 OpenCode 使用 GLM-5。
