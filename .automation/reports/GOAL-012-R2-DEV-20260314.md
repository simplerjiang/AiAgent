# GOAL-012-R2 Development Report / 开发报告

## EN
- Task: Replace the stock chart engine behind `frontend/src/modules/stocks/charting/**` with `klinecharts` while keeping the parent-facing `StockCharts.vue` contract unchanged.
- Code changes:
  - Added `frontend/src/modules/stocks/charting/klinechartsRegistry.js` for indicator/overlay registry management and future signal-marker mounting.
  - Replaced `frontend/src/modules/stocks/charting/useStockChartAdapter.js` internals from `lightweight-charts` to `klinecharts`.
  - Updated `frontend/src/modules/stocks/StockCharts.spec.js` to mock `klinecharts` loader/indicator/overlay behavior.
  - Pinned `klinecharts` to exact `10.0.0-beta1` in `frontend/package.json` and `frontend/package-lock.json`.
  - Removed the legacy `lightweight-charts` dependency from the frontend package manifest and lockfile.
  - Synced `README.md`, `.automation/tasks.json`, `.automation/state.json`, and repo rules.
- Validation:
  - Command: `npm --prefix frontend run test:unit -- --run src/modules/stocks/StockCharts.spec.js`
  - Result: pass
  - Command: `npm --prefix frontend run test:unit -- src/modules/stocks/StockInfoTab.spec.js`
  - Result: pass
  - Command: `npm --prefix frontend run build`
  - Result: pass
  - Command: Browser MCP on backend-served `http://localhost:5119/`
  - Result: stock query for `600519` loaded live data successfully; `分时图` and `月K图` both switched successfully; active chart pane rendered `canvas` nodes and the placeholder disappeared
- Regression follow-up:
  - Fixed K-line timestamps from Unix seconds to Unix milliseconds so the x-axis no longer falls into 1970/1971 on `klinecharts`.
  - Normalized minute-line `volume` from cumulative values to per-bar deltas, keeping display units in hands.
  - Revalidated live higher-timeframe payloads: `interval=month` returned 60 K-lines and `interval=year` returned 26 K-lines; Browser MCP confirmed `月K图` and `年K图` both render without the placeholder.
  - Converted the chart legend chips from static labels into interactive toggles. Minute view now controls `price` / `volume` / `baseLine` / `aiLevels`, and K-line views control `price` / `volume` / `ma5` / `ma10` / `aiLevels` through shared adapter visibility state.
  - Extended `StockCharts.spec.js` so the toggle regression is locked with runtime assertions against indicator removal, overlay removal, and active/inactive chip classes.
- Additional validation:
  - Command: `npm run test:unit -- src/modules/stocks/StockCharts.spec.js src/modules/stocks/StockInfoTab.spec.js`
  - Result: pass (42/42)
  - Command: `npm run build`
  - Result: pass
  - Command: Browser MCP on backend-served `http://localhost:5119/`
  - Result: loaded `sz000021`, toggled K-line `MA10` + `AI 价位` to inactive, switched to `分时图`, then toggled `分时` / `量能` / `昨收基线` / `AI 价位` to inactive; visible button state changed correctly and browser console remained error-free
- Issues / risks:
  - `klinecharts` is still a beta dependency (`10.0.0-beta1`), so future UI/browser validation should continue to focus on crosshair behavior, pane layout, and runtime regressions after dependency bumps.
  - Browser session logs still contained two stale historical `GET /api/stocks/plans?take=20 => 500` entries from an earlier page state, but fresh network traffic for the current backend-served run returned `200` and backend logs showed no new exception records.
- Conclusion:
  - The controlled replacement is technically viable, is now the active chart path, and remains isolated behind the existing adapter boundary.

## ZH
- 任务：在保持 `StockCharts.vue` 父层 contract 不变的前提下，将 `frontend/src/modules/stocks/charting/**` 内的股票图表引擎替换为 `klinecharts`。
- 本轮改动：
  - 新增 `frontend/src/modules/stocks/charting/klinechartsRegistry.js`，统一管理指标/叠加线 registry，并为未来信号标记层预留挂载入口。
  - 将 `frontend/src/modules/stocks/charting/useStockChartAdapter.js` 内核从 `lightweight-charts` 切换为 `klinecharts`。
  - 改写 `frontend/src/modules/stocks/StockCharts.spec.js`，改为模拟 `klinecharts` 的 loader、indicator、overlay 行为。
  - 在 `frontend/package.json` 与 `frontend/package-lock.json` 中将 `klinecharts` 精确锁定为 `10.0.0-beta1`。
  - 从前端依赖与 lockfile 中移除旧 `lightweight-charts` 包。
  - 同步更新 `README.md`、`.automation/tasks.json`、`.automation/state.json` 与仓库规则。
- 验证：
  - 命令：`npm --prefix frontend run test:unit -- --run src/modules/stocks/StockCharts.spec.js`
  - 结果：通过
  - 命令：`npm --prefix frontend run test:unit -- src/modules/stocks/StockInfoTab.spec.js`
  - 结果：通过
  - 命令：`npm --prefix frontend run build`
  - 结果：通过
  - 命令：在后端托管页面 `http://localhost:5119/` 执行 Browser MCP 查股与图表切换验收
  - 结果：输入 `600519` 后可正常加载实时详情；`分时图` 与 `月K图` 都能切换成功；活动图表 pane 已渲染 `canvas` 且占位文案消失
- 回归修复补充：
  - 已把 K 线时间戳从 Unix 秒改为 Unix 毫秒，修复 `klinecharts` 横轴落到 1970/1971 的问题。
  - 已把分时成交量从累计值转换为逐 bar 增量，并统一按“手”维度显示。
  - 已重新验证高周期真实数据：`interval=month` 返回 60 根 K 线、`interval=year` 返回 26 根 K 线；Browser MCP 已确认 `月K图` 与 `年K图` 均可渲染且不再出现占位态。
  - 已将图表图例从静态文案改为可点击开关。分时图现在可切换 `分时/量能/昨收基线/AI 价位`，K 线视图可切换 `蜡烛/量能/MA5/MA10/AI 价位`，统一通过 adapter 的 visibility 状态驱动样式、指标和 overlay 显隐。
  - 已扩展 `StockCharts.spec.js`，对图例点击后的指标移除、overlay 移除以及按钮 active/inactive 样式状态做回归断言，防止再次退化为“只有文字，没有交互”。
- 追加验证：
  - 命令：`npm run test:unit -- src/modules/stocks/StockCharts.spec.js src/modules/stocks/StockInfoTab.spec.js`
  - 结果：通过（42/42）
  - 命令：`npm run build`
  - 结果：通过
  - 命令：在后端托管页面 `http://localhost:5119/` 执行 Browser MCP 图例点击验收
  - 结果：加载 `sz000021` 后，已实测将日K `MA10`/`AI 价位` 切为 inactive，再切到 `分时图` 将 `分时`/`量能`/`昨收基线`/`AI 价位` 全部切为 inactive；按钮可见状态变化正确，前端控制台无报错
- 问题 / 风险：
  - `klinecharts` 仍是 beta 版本（`10.0.0-beta1`），后续 Browser MCP/人工验收需要继续重点观察交叉线、分 pane 布局和依赖升级后的运行时回归。
  - 浏览器会话中仍保留了更早一次页面状态的两条 `GET /api/stocks/plans?take=20 => 500` 历史错误，但本次后端托管页重新加载后的新请求已返回 `200`，且后台日志未发现新的异常记录。
- 结论：
  - 本次受控替换在技术上可行，现已成为当前图表主路径，并且仍被限制在既有适配层内，回滚成本可控。