# GOAL-012-R3 Development Report / 开发报告

## EN
- Task: Start GOAL-012-R3 by replacing the hardcoded legend model with a unified chart strategy registry behind `frontend/src/modules/stocks/charting/**`, while keeping the parent `StockCharts.vue` contract unchanged.
- Code changes:
  - Added `frontend/src/modules/stocks/charting/chartStrategyRegistry.js` as the Phase A strategy registry foundation. It now owns per-view strategy definitions, grouped chip metadata, visibility defaults, render-plan aggregation, and the first custom indicator registrations.
  - Added `frontend/src/modules/stocks/charting/chartPanes.js` to centralize pane IDs for the registry and renderer.
  - Simplified `frontend/src/modules/stocks/charting/chartViews.js` so views no longer hardcode legend chips.
  - Refactored `frontend/src/modules/stocks/charting/klinechartsRegistry.js` to consume normalized render plans instead of hand-written `MA/VOL/AI/base-line` branches.
  - Updated `frontend/src/modules/stocks/charting/useStockChartAdapter.js` to register custom strategies, build render plans per active view, and pass those plans into the chart renderer.
  - Updated `frontend/src/modules/stocks/StockCharts.vue` to render grouped strategy controls by category (`基础图层 / 趋势策略 / 动量指标 / 信号标记`) using the shared per-view visibility state.
  - Extended `frontend/src/modules/stocks/StockCharts.spec.js` with grouped-control and registry-driven coverage for `MA60`, `BOLL`, `VWAP`, and `ORB`, while keeping existing chart behavior tests green.
- Implemented strategy set in this phase:
  - Core: `price`, `volume`, `AI 价位`, `昨收基线`
  - Trend: `MA5`, `MA10`, `MA20`, `MA60`, `VWAP`, `BOLL`, `Donchian`
  - Oscillator: `MACD`, `RSI`, `KDJ`
  - Signal: `ORB`
- Validation:
  - Command: `npm run test:unit -- src/modules/stocks/StockCharts.spec.js src/modules/stocks/StockInfoTab.spec.js`
  - Result: pass (45/45)
  - Command: `npm run build`
  - Result: pass
  - Command: Browser MCP on backend-served `http://localhost:5119/`
  - Result: loaded `sz000021`, verified grouped K-line controls, toggled `MA60` and `MACD`, switched to `分时图`, and verified `VWAP` plus `ORB` active-state toggles; fresh browser console errors remained `0`
- Usability follow-up:
  - Added per-strategy help metadata (`accentColor`, `description`, `interpretation`, `usage`) to the chart strategy registry so active strategies can explain themselves consistently.
  - Added floating strategy badges inside the chart shell to display active line/indicator names at the chart head, with hover popovers for `介绍 / 解释 / 用法`.
  - Added a `隐藏小标 / 显示小标` toggle so users can suppress the floating badges when they want a cleaner chart view.
  - Expanded the indicator line palette so MA5 / MA10 / MA20 / MA60 are easier to distinguish visually.
- Additional validation:
  - Command: `npm --prefix .\frontend run test:unit -- src/modules/stocks/StockCharts.spec.js src/modules/stocks/StockInfoTab.spec.js`
  - Result: pass (47/47)
  - Command: `npm --prefix .\frontend run build`
  - Result: pass
  - Command: Browser MCP on backend-served `http://localhost:5119/`
  - Result: loaded `sz000021`, confirmed the new `隐藏小标` toggle, hovered the active `MA5` badge and observed the `介绍 / 解释 / 用法` popover, switched to `分时图`, hovered the active `VWAP` badge and observed its popover, then verified browser console errors remained `0`
- Issues / risks:
  - `ATR`, `TD Sequential`, cross-family markers, breakout/fake-breakout, gap, divergence, and VWAP strength markers are still pending later GOAL-012-R3 phases.
  - The startup script rerun was sensitive to terminal directory drift; absolute-path invocation avoided the false “file not found” failure.
- Conclusion:
  - GOAL-012-R3 has started successfully. Phase A is now landed with a working grouped registry model and an initial real strategy batch validated in unit tests, production build, and backend-served Browser MCP.
  - The requested readability improvement is also complete: active strategies now expose color-coded floating labels, hover help, and a global hide/show toggle without changing the parent chart contract.

## ZH
- 任务：启动 GOAL-012-R3，在保持 `StockCharts.vue` 父层 contract 不变的前提下，把原先硬编码图例模型升级为统一的图表策略注册表，并继续限制在 `frontend/src/modules/stocks/charting/**` 边界内。
- 本轮改动：
  - 新增 `frontend/src/modules/stocks/charting/chartStrategyRegistry.js` 作为 Phase A 策略注册表底座，统一管理按 view 的策略定义、分组按钮元数据、默认显隐状态、渲染计划聚合，以及首批自定义指标注册。
  - 新增 `frontend/src/modules/stocks/charting/chartPanes.js`，集中管理 pane ID，避免 registry 与 renderer 反复散落硬编码。
  - 精简 `frontend/src/modules/stocks/charting/chartViews.js`，视图层不再硬编码 legend/button 列表。
  - 重构 `frontend/src/modules/stocks/charting/klinechartsRegistry.js`，改为消费统一 render plan，而不是继续手写 `MA/VOL/AI/昨收线` 分支。
  - 更新 `frontend/src/modules/stocks/charting/useStockChartAdapter.js`，在活动视图下注册自定义策略、构建 render plan，并把结果传给图表 renderer。
  - 更新 `frontend/src/modules/stocks/StockCharts.vue`，将图表控制区升级为按类别分组的策略按钮（`基础图层 / 趋势策略 / 动量指标 / 信号标记`），统一由按 view 的 visibility state 驱动。
  - 扩展 `frontend/src/modules/stocks/StockCharts.spec.js`，新增 `MA60`、`BOLL`、`VWAP`、`ORB` 的分组控制与 registry 行为断言，同时保证既有图表行为测试继续通过。
- 本阶段已落地的策略：
  - 基础图层：`price`、`volume`、`AI 价位`、`昨收基线`
  - 趋势策略：`MA5`、`MA10`、`MA20`、`MA60`、`VWAP`、`BOLL`、`Donchian`
  - 动量指标：`MACD`、`RSI`、`KDJ`
  - 信号标记：`ORB`
- 验证：
  - 命令：`npm run test:unit -- src/modules/stocks/StockCharts.spec.js src/modules/stocks/StockInfoTab.spec.js`
  - 结果：通过（45/45）
  - 命令：`npm run build`
  - 结果：通过
  - 命令：在后端托管页面 `http://localhost:5119/` 执行 Browser MCP 验收
  - 结果：加载 `sz000021` 后，已实测日K分组策略按钮，切换 `MA60` 与 `MACD`；再切到 `分时图`，确认 `VWAP` 与 `ORB` 的 active 状态可切换，且本次新页面会话前端控制台错误数保持 `0`
- 可读性补强：
  - 已为每个策略补齐 help 元数据（`accentColor`、`description`、`interpretation`、`usage`），让 active 图层能统一输出可解释信息。
  - 已在图表区新增浮动策略小标，显示当前激活的线/指标名称，并在鼠标悬浮时弹出 `介绍 / 解释 / 用法`。
  - 已新增 `隐藏小标 / 显示小标` 总开关，用户可在需要纯净图面时一键关闭。
  - 已扩展指标线配色，使 MA5 / MA10 / MA20 / MA60 的区分度更高。
- 追加验证：
  - 命令：`npm --prefix .\frontend run test:unit -- src/modules/stocks/StockCharts.spec.js src/modules/stocks/StockInfoTab.spec.js`
  - 结果：通过（47/47）
  - 命令：`npm --prefix .\frontend run build`
  - 结果：通过
  - 命令：在后端托管页面 `http://localhost:5119/` 执行 Browser MCP 验收
  - 结果：加载 `sz000021` 后，已确认图表区出现 `隐藏小标` 按钮；悬浮 `MA5` 活动小标时会弹出 `介绍 / 解释 / 用法`；切到 `分时图` 后悬浮 `VWAP` 活动小标同样可弹出说明，且前端控制台错误数保持 `0`
- 问题 / 风险：
  - `ATR`、`神奇九转/TD Sequential`、金叉死叉家族、突破/假突破、缺口、量价背离、VWAP 强弱等仍属于 GOAL-012-R3 后续阶段，尚未在本轮实现。
  - 本地启动脚本重跑时受终端 cwd 漂移影响，使用绝对路径调用后已规避该类“脚本不存在”的伪失败。
- 结论：
  - GOAL-012-R3 已成功起步。当前 Phase A 已落地可运行的分组 registry 模型与首批真实策略，并已通过单测、生产构建与后端托管页面 Browser MCP 验收。
  - 你要求的“可区分颜色 + 浮动小标 + hover 介绍/解释/用法 + 总开关”也已经完成，且没有破坏现有父层 contract。
