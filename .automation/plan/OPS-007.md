# OPS-007 Plan - Unified AI Request Conversation Logging

1. Inspect all LLM invocation paths (`LlmService`, `/api/llm/chat`, `/api/llm/chat/stream`) and confirm current logging coverage.
2. Add unified audit logs at service layer with stable trace ID and lifecycle stages (request/response/error).
3. Add stream endpoint audit logs so streaming requests and assembled streamed output are also captured.
4. Apply safe log formatting (newline escaping + max-length truncation) to keep logs analyzable and bounded.
5. Run backend unit tests and record results in bilingual report.
6. Update `.automation/tasks.json`, `.automation/state.json`, and `README.md` to keep automation and docs in sync.
